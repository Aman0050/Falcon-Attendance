services:
  db:
    image: postgis/postgis:15-3.3
    environment:
      POSTGRES_USER: falcon_user
      POSTGRES_PASSWORD: falcon_password
      POSTGRES_DB: falcon_office
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
